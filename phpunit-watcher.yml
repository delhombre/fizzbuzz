watch:
  directories:
    - src
    - tests
  fileMask: "*.php"
notifications:
  passingTests: false
  failingTests: false
phpunit:
  binaryPath: vendor/bin/phpunit
  arguments: "--stop-on-failure --color=always --testdox --no-coverage"
  timeout: 180
hideManual: true